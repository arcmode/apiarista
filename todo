-- add command to create admin consumer configuration

	- create consumer and get :id and secret

	- create /data/config/consumers.js

-- deal with throttle

-- take db name from pkg instead of basename(process.cwd())